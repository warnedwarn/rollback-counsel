# RollbackCounsel

Uses validator consensus over a release note and incident record to authorize or hold a software rollback. Only the proposal owner can execute an authorization; expired unexecuted proposals may be lapsed by anyone.

Run `PYTHONUTF8=1 genvm-lint contracts/contract.py` and `python -m pytest -q`.
